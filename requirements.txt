# Core runtime
telethon==1.36.0

# MongoDB (+ SRV support for mongodb+srv URIs)
pymongo==4.8.0
dnspython==2.6.1

# Dates, time parsing, and time zones
python-dateutil==2.9.0.post0
tzdata>=2024.1

# Networking + HTTP client (for image downloads)
aiohttp==3.10.5
certifi>=2024.6.2

# Images (JPEG/WebP capable wheels on Linux)
pillow==10.4.0
